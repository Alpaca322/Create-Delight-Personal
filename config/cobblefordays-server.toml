#Server configuration settings
[server]

	[server.1]
		# Default: 40
		# Range: > 1
		interval = 40
		# Default: 1
		# Range: > 1
		count = 1
		# Default: 64
		# Range: > 1
		max = 64
		pushes = true

	[server.2]
		# Default: 20
		# Range: > 1
		interval = 20
		# Default: 1
		# Range: > 1
		count = 1
		# Default: 128
		# Range: > 1
		max = 128
		pushes = true

	[server.3]
		# Default: 10
		# Range: > 1
		interval = 10
		# Default: 1
		# Range: > 1
		count = 1
		# Default: 256
		# Range: > 1
		max = 256
		pushes = true

	[server.4]
		# Default: 5
		# Range: > 1
		interval = 5
		# Default: 1
		# Range: > 1
		count = 1
		# Default: 512
		# Range: > 1
		max = 512
		pushes = true

	[server.5]
		# Default: 1
		# Range: > 1
		interval = 1
		# Default: 1
		# Range: > 1
		count = 1
		# Default: 1024
		# Range: > 1
		max = 1024
		pushes = true

	[server.6]
		# Default: 1
		# Range: > 1
		interval = 1
		# Default: 4
		# Range: > 1
		count = 4
		# Default: 2048
		# Range: > 1
		max = 2048
		pushes = true
